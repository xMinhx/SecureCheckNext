[pytest]
addopts = --nomigrations --reuse-db -m "not nvd_integration and not e2e"
DJANGO_SETTINGS_MODULE = securecheckplus.settings
markers =
    nvd_integration: tests that call the real NVD API (requires internet + API key)
    e2e: end-to-end tests that require a running PostgreSQL instance